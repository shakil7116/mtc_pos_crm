node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Secrets — never commit
.env
.env.local
*.local

# Replit platform files
.config/

# Logs / temp
*.log
/tmp

# Real business data — never commit (repo is public)
stock-import.csv
/*-import.csv

# Database backups — the ENTIRE business record. Never commit.
backups/
*.json.gz
